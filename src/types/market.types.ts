export enum MarketType {
  vault_token = 'vault_token',
  robinhood_token = 'robinhood_token',
}

/** Distinguishes Robinhood JSON seed categories (memecoins / RWAs / NFTs). */
export enum MarketTokenKind {
  memecoin = 'memecoin',
  rwa = 'rwa',
  nft = 'nft',
}
