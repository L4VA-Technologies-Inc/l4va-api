export enum AssetType {
  NFT = 'nft',
  FT = 'ft',
  ADA = 'ada',
}

export enum AssetStatus {
  PENDING = 'pending',
  LOCKED = 'locked',
  RELEASED = 'released',
}

export enum AssetOriginType {
  ACQUIRED = 'acquired',
  CONTRIBUTED = 'contributed',
}
