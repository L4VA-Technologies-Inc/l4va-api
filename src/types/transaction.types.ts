
export enum TransactionStatus {
  pending = 'pending',
  submitted ='submitted',
  confirmed = 'confirmed',
  failed = 'failed'
}

export enum TransactionType {
  mint = 'mint',
  payment = 'payment',
  contribute = 'contribute',
  burn ='burn',
  swap = 'swap',
  stake ='stake'
}
